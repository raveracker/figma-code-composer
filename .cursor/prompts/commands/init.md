# /init — Cursor command

When the user types `/init` in Cursor agent mode, follow `.cursor/prompts/wizard.md` step-by-step. Do not invoke any other agent or command first.

Trigger phrases: `/init`, "set up figma-pipeline", "configure the pipeline", "run the wizard".

Argument: `--re-detect` skips identity and refreshes framework + CSS detection only.
