# Codex project-detector

Verbatim mirror of `.claude/agents/project-detector.md`. Codex variant uses subprocess `Glob`/`Read`/`Grep` shell calls instead of native tools — but the detection rules and output schema are byte-identical.

See `.claude/agents/project-detector.md` for the full protocol.
