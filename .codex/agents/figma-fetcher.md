# Codex figma-fetcher

Mirror of `.claude/agents/figma-fetcher.md`. Codex variant uses the Figma MCP server entries from `.mcp.json` directly (no built-in `mcp__figma__*` tool prefix — Codex resolves MCP tools by server name).

Output, write scope, and contract rules are otherwise byte-identical.
