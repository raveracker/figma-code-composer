# `codex run figma-tokens <figma-url>`

Mirror of `.claude/commands/figma-tokens.md`. `scope: "tokens-only"`.

Same two entry points as `figma-build.md` — `./codex-run figma-tokens '<url>'` (nested) or **inline** inside an interactive `codex` session (then run `.codex/hooks/post-command.sh figma-tokens`). See `.codex/README.md` § _Two ways to run_.
