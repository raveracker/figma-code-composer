# `codex run figma-icons <figma-url>`

Mirror of `.claude/commands/figma-icons.md`. `scope: "icons-only"`.

Same two entry points as `figma-build.md` — `./codex-run figma-icons '<url>'` (nested) or **inline** inside an interactive `codex` session (then run `.codex/hooks/post-command.sh figma-icons`). See `.codex/README.md` § _Two ways to run_.
