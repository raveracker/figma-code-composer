# `codex run figma-update <figma-url> [layerHint]`

Mirror of `.claude/commands/figma-update.md`. Same as figma-build but with `intent: "update"`.

Same two entry points as `figma-build.md` — `./codex-run figma-update '<url>'` (nested) or **inline** inside an interactive `codex` session (then run `.codex/hooks/post-command.sh figma-update`). See `.codex/README.md` § _Two ways to run_.
